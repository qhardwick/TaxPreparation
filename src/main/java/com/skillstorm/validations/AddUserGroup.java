package com.skillstorm.validations;

// Empty interface used for validation group. Requires email field to be present when adding a new user without
// enforcing the rest of the constraints.
public interface AddUserGroup {
}

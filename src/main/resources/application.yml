#Configure the host server:
server:
  port: 8080
  servlet:
    context-path: /taxes

#Configure MongoDB:
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: taxes
      auto-index-creation: true